import m, { ClassComponent, CVnode } from "mithril";
import { IFile } from "../interface/widget";
export declare class FileOpen implements ClassComponent<IFile> {
    view({ attrs }: CVnode<IFile>): m.Vnode<any, any>;
}
