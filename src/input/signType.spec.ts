import "../mockBrowser";
// tslint:disable no-var-requires
const o = require("ospec");

// import m from "mithril";

// import { SignType } from "./signType";
// import { renderText } from "./signType";

o.spec("SignType", () => {

	// o("create/remove", () => {
	// 	const root = window.document.createElement("div");
	// 	m.mount(root, {
	// 		view: () => m(SignType, {
	// 			onSet: (_) => null,
	// 			onCancel: () => null
	// 		})
	// 	});
	// 	o(root.childNodes.length).equals(2);
	// 	m.mount(root, null);
	// 	o(root.childNodes.length).equals(0);
	// });

	// o("render", () => {
	// 	const dataUrl = renderText("test");
	// 	o(dataUrl.length > 1).equals(true);
	// });

});
